help() {
  grep "^#/ " "_{file_name}_" | sed 's/#\/ //g'
}

# [[ "$1" == "--help" ]] ||
#   [[ "$1" == "-h" ]] ||
#   [[ "$1" == "-?" ]] ||
#   [[ "$1" == "help" ]] ||
#   [[ "$1" == "h" ]] ||
#   [[ "$1" == "?" ]] &&
#   help && exit 0
