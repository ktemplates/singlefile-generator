
help() {
  grep "^#/" "$${file_name}" | sed 's/#\/ //g'
}

# [[ "$1" == "--help" ]] ||
#   [[ "$1" == "-h" ]] ||
#   [[ "$1" == "-?" ]] ||
#   [[ "$1" == "help" ]] ||
#   [[ "$1" == "h" ]] ||
#   [[ "$1" == "?" ]] &&
#   help
