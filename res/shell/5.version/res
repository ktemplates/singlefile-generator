version() {
  grep "^#// " "_{file_name}_" | sed 's/#\/\/ //g'
}

# [[ "$1" == "--version" ]] ||
#   [[ "$1" == "-v" ]] ||
#   [[ "$1" == "version" ]] ||
#   [[ "$1" == "v" ]] &&
#   version && exit 0
