# move to script directory

# Option 1
cd "$(dirname "$0")" || exit 1

# Option 2
# cd "$(dirname "$(realpath "$0")")" || exit 1

# Option 3
# handle symlink
# real="$0"
# [ -h "$real" ] && real="$(readlink "$real")"
# cd "$(dirname "$real")" || exit 1
